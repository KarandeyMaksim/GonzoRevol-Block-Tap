# Audio credits

- `music_loop.wav` — supplied directly by the client for this project.
- `win.wav` — supplied directly by the client for this project ("Game won!").
- `lose.wav`, `tap.wav`, `place.wav`, `coin.wav`, `wheel_spin.wav` —
  programmatically generated placeholder SFX (owned, no licensing restriction).
  Swap these for licensed/produced SFX if desired.
